# Copy this file to .env and load it into your shell before running Mix commands.
# `source .env` works if your file only contains KEY=value lines like these.

DATABASE_URL=ecto://postgres:postgres@localhost/bravo_credit_dev
TEST_DATABASE_URL=ecto://postgres:postgres@localhost/bravo_credit_test
SECRET_KEY_BASE=replace_me_with_mix_phx_gen_secret
GUARDIAN_SECRET_KEY=replace_me_with_a_long_random_string
CLOAK_KEY=0123456789ABCDEF0123456789ABCDEF
PHX_HOST=localhost
PORT=4000
