country_code: ES
country_name: Spain
currency: EUR

document:
  type: DNI
  validator: dni

rules: []

provider:
  adapter: bank_es
  timeout_ms: 6000

state_transitions:
  pending:
    - provider_processing
    - cancelled
  provider_processing:
    - evaluating
    - cancelled
  evaluating:
    - approved
    - rejected
    - in_review
  in_review:
    - approved
    - rejected
    - cancelled
  approved:
    - cancelled
  rejected: []
  cancelled: []

review:
  high_amount_threshold: "30000.00"
  min_auto_approval_score: 650
